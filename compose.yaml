services:
  css-tweaks:
    build:
      context: .
      args:
        - GIT_COMMIT=${GIT_COMMIT:-dev}
        - GIT_BRANCH=${GIT_BRANCH:-unknown}
        - GIT_ORIGIN=${GIT_ORIGIN:-unknown}
    container_name: css_snippets
    ports:
      - "8080:8080"
    restart: unless-stopped